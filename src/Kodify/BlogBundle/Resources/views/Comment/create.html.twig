<div class="new-comment-area">
    <div class="comment-form">
        {{ form_start(form, {'attr': {'class': 'create-form'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
